<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zk.warehouse.information.management.web.admin.dao.TbContentCategoryDao">
    <sql id="tbContentCategoryColumns">
        a.id,
        a.parent_id AS parentId,
        a.is_parent AS isParent,
        a.name,
        a.status,
        a.sort_order AS sortOrder,
        a.created,
        a.updated
    </sql>

    <select id="selectAll" resultType="TbContentCategory">
        SELECT
            <include refid="tbContentCategoryColumns"/>
        FROM
            tb_content_category AS a
        ORDER BY
            a.parent_id ASC ,
            a.is_Parent ASC ,
            a.sort_order DESC
    </select>
</mapper>